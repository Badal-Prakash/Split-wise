import { connectDb } from "@/lib/db"; import { Expense } from "@/models/Expense"; import { Group } from "@/models/Group"; import { ok, fail } from "@/lib/http"; import { currentUserId } from "@/lib/auth";
export async function GET(){await connectDb(); const uid=await currentUserId(); if(!uid)return fail("Unauthorized",401); const groups=await Group.find({members:uid}).select("_id").lean(); const groupIds=groups.map(group=>group._id); const rows=await Expense.aggregate([{ $match:{deletedAt:{$exists:false},$or:[{paidBy:uid},{"payers.userId":uid},{"splitBetween.userId":uid},{groupId:{$in:groupIds}}]}},{ $group:{ _id:{ y:{$year:"$date"}, m:{$month:"$date"}}, total:{$sum:"$amount"}, count:{$sum:1}}},{ $sort:{"_id.y":1,"_id.m":1}}]); return ok(rows.map(row=>({month:`${row._id.y}-${String(row._id.m).padStart(2,"0")}`,total:row.total,count:row.count})));}
